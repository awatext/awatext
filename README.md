# awatext

这是一个轻量的静态导航站示例。包含响应式布局、搜索过滤与主题切换。

如何使用
- 将仓库克隆到本地后，直接用浏览器打开 `index.html` 即可。
- 编辑 `js/script.js` 中的 `data` 数组来添加或修改分组与链接。
- 编辑 `css/style.css` 来调整主题或配色。

文件说明
- `index.html`：主页面。
- `css/style.css`：样式文件。
- `js/script.js`：渲染与交互逻辑（搜索、主题切换）。

